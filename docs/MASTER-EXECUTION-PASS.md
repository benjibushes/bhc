# MASTER EXECUTION PASS — the one systemized checklist to "complete end to end"

> Committed 2026-07-14 late. PURPOSE: every remaining work item in ONE repo-resident,
> session-proof document. If the AI session dies, credits lapse, or an agent stalls,
> ANY future session executes from here cold: work top to bottom, one PR per phase,
> gauntlet each (tsc clean / npm test green / REAL next build exit 0), verify-then-merge.
> Sources: rancher-journey audit (58 agents, adversarially verified), checkout audit,
> dashboard audit — all specs inlined below so nothing depends on session files.

## STATE (as of commit)
- Waves LANDED: A=#382 (7 criticals), B=#383 (onboarding path). Sprints #374/#376, hardening #369-381 all live.
- Wave C IN-FLIGHT on branch fix/rancher-wave-c (14 items; 5 committed at time of writing). If its PR never lands, its scope = the in-flight-marked specs below; branch commits recoverable via `git log origin/main..fix/rancher-wave-c`.
- Tests at last green: 1070. Prod deploy verified at #383.

## PHASE 1 — land Wave C (or re-execute its scope)
If PR fix/rancher-wave-c is open: verify (merge main in, re-gauntlet, spot-check money-adjacent diffs), then squash-merge + confirm signature symbols on main.

## PHASE 2 — Wave D: the leftovers (one PR)
1. Rescue-nudge whitelist: sendRancherLeadReminder + the day-14 stale prompt get templateNames + whitelist entries (spec 3 below).
2. Checkout-audit pre-ads tail: price-drift 409 auto-recover in PaymentForm; MIN/MAX price bound at charge time in computeProductCharge; Connect-active gate on PDP + checkout loaders (kill dead deep-links); qty clamp to Orders Left on the checkout page.
3. Money accuracy: settlement fetches balance_transaction, records ACTUAL net + fee; alert when rancher true net < promised Base.

## PHASE 3 — activation layer (the flywheel)
1. ACTIVATION QUEUE /admin/activation: all 80 ranchers ranked demand-state-weight x proximity-to-active. Audit buckets: [B] applied-untouched 12 · [C] call-scheduled 2 · [D] call-complete 17 · [E] docs-sent 12 · [F] signed-not-live 1 · [G] live-no-Connect 9 · [H] Connect-stuck 5 · [I] active 6. Next-action + contact inline; weekly Telegram "call these 3".
2. Shop subscribe (recurring rail #1) — design first: monthly jerky/box on the product rail; decide subscription-on-connected-acct vs platform-owned + transfers.
3. Testimonial surfacing: wins strip on rancher pages + share checkout.

## BEN-OPS (cannot be coded)
- SALES TAX decision: Stripe Tax on the product rail (mirror brand rail) OR documented rancher-merchant-of-record exemption.
- The 9 live-no-Connect ranchers: pause routing or chase to connect. JC's Ranch NC first (12 stranded referrals). Use bhc-mutation-guardrails.
- 29 warm stalled ranchers = the first onboarding wave: docs-send the 17 [D], e-sign nudge the 12 [E].
- 5 Bar Beef CA (714-749-5717, approved, needs only Stripe) + Ashcraft TX (un-pause + approve). buyhalfcow.co purchase. Nurture + waiting-activation flips after reading dry-run Telegrams. Meta envs when 2-3 states have supply.

## VERIFICATION RITUAL (every phase)
`npx tsc --noEmit` clean · `npm test` all green · REAL `npm run build` exit 0 (retry once on transient Airtable prerender timeout) · merge main before PR · after squash-merge grep main for a signature symbol · confirm Vercel deploy READY · curl the changed surface live.

---
## INLINED SPECS (rancher-audit importants — self-contained)

### 1. [in-flight (committed in wave C)] Stripe pending balance fetched but never rendered — fresh deposit money looks like $0
**Effort:** tiny · **Files:** app/rancher/page.tsx
**Why:** /api/rancher/payouts returns pendingCents (route.ts:100) and the dashboard types it (page.tsx:209), but zero surfaces render it. Right after a first deposit, funds sit pending ~2 business days while the money strip shows 'Ready to pay out $0' next to 'Deposits collected $X' — the exact moment trust is thinnest. Top 'where is my money?' support-ping generator.
**Fix:** In app/rancher/page.tsx derive pendingDollars next to availableDollars (:1784): `payouts?.pendingCents != null ? Math.round(payouts.pendingCents/100) : null`; thread into HomeTab props; in the money-strip grid (:5594) render a tile when pendingDollars > 0: value `$${pendingDollars.toLocaleString()}`, label 'On its way to your bank — settles in ~2 business days' (append `· arrives ~{nextPayoutLabel}` when present). Include pendingDollars in the hasMoney check (:5509).

### 2. [DONE in Wave A #382] Orders fetch failure is a silent false-empty at two layers — paid orders vanish with no error
**Effort:** tiny · **Files:** app/rancher/ProductsTab.tsx
**Why:** Client: non-OK /api/rancher/orders is swallowed (ProductsTab.tsx:136-143, empty catch) and the section only renders when orders.length > 0 — a 500/expired session renders exactly like 'no orders'. Server: GET wraps getAllRecords in .catch(() => []) (orders/route.ts:68-71), converting an Airtable outage into 200-empty. A rancher clicking through the one-shot SLA nudge during a blip concludes there's nothing to ship. Same false-empty class as the inbox 401 bug fixed in #374 — inbox only, orders missed.
**Fix:** Server: drop the `.catch(() => [])` in GET app/api/rancher/orders/route.ts — catch, log, return 502 `{ error: 'could not load your orders — refresh in a minute.' }` (mirror the #374 inbox fix). Client: add `ordersErr` state in ProductsTab; set on !res.ok (401/403 → redirect to /rancher/login like inbox) or on throw; render above the products list with a retry button that re-runs the fetch.

### 3. [OPEN - fold into Wave D] Rancher rescue-nudge emails are silently eaten by the 3/week cap while throttle stamps burn and Telegram reports them as sent
**Effort:** tiny · **Files:** lib/emailFrequencyGuard.ts
**Why:** Neither 'sendRancherLeadReminder' (day-2 untouched-lead email) nor the day-14 stale prompt (generic sendEmail, no templateName → capped) is whitelisted, and the 3/week cap counts ALL sends — an active rancher (intro + deposit-paid + thread mirror) trivially exhausts it. referral-chasup stamps 'Rancher Reminded At' BEFORE the send and increments counters without checking the result (guardedSend returns success:false without throwing), so lifetime Chase Count slots burn with nothing delivered and the Telegram digest lies. Exactly the ranchers with the most active pipelines lose their rescue nudges.
**Fix:** 1) Add 'sendRancherLeadReminder' to TRANSACTIONAL_WHITELIST in lib/emailFrequencyGuard.ts (cadence owned by the cron's own 4-day throttle — mirrors the sendRancherDepositPaid precedent at :192). 2) Give the day-14 prompt templateName 'rancher_stale_prompt' at app/api/cron/referral-chasup/route.ts:583 and whitelist it (7-day Last Chased At throttle owns cadence). 3) Also whitelist sendRancherLeadNudge (rancher-followup :364 — same class). 4) Check results: `const r = await sendRancherLeadReminder(...); if (r?.success) rancherReminders++; else skipReasons['reminder-suppressed']++;` same for stalePromptsFired. Keep stamp-before-send ordering (dup-safety).

### 4. [in-flight (committed in wave C)] Admin pause is completely silent to the rancher
**Effort:** tiny · **Files:** app/api/admin/ranchers/[id]/pause/route.ts
**Why:** POST /api/admin/ranchers/[id]/pause flips Active Status='Paused' (stopping all new leads) and Telegrams only the operator (:36-43). No email, no push — and pausing also mutes every other email rail (rancher-followup/reactivation exclude Paused). A rancher who doesn't log in never learns lead flow was shut off or why. Live case: Ashcraft TX Paused while carrying 33 active referrals, plausibly unaware.
**Fix:** In app/api/admin/ranchers/[id]/pause/route.ts after updateRecord succeeds: (1) sendEmail to rancher['Email'] — subject 'We paused new buyer leads to {Ranch Name}', include the reason string when provided, state that existing conversations continue and the page stays live, link /rancher to resume or reply to discuss (truth-log per #374 pattern, whitelisted templateName). (2) Best-effort sendRancherPush(id, 'New leads paused', reason). (3) Optional: morning-pulse probe listing ranchers Paused >7d with active referrals so long silent pauses surface to Ben.

### 5. [in-flight (wave C item 9)] New routed lead fires email + opt-in push but never SMS — the platform's most time-sensitive rancher notification is single-channel-to-inbox
**Effort:** small · **Files:** lib/smsEvents.ts
**Why:** lib/smsEvents.ts:41 defines 'deposit_paid_rancher' as the ONLY rancher SMS event — a rancher with a Phone on file and ENABLE_SMS on gets nothing on their phone when a buyer routes. Intro email delivered-to-spam is invisible (rollback fires only on hard errors), push requires PWA opt-in (few have). The day-2 rescue is email-only and capped (see nudge fix). Live 'Intro Sent' leads rot for weeks; the deposit-paid SMS rail already exists and works.
**Fix:** 1) lib/smsEvents.ts: add 'new_lead_rancher' to RancherSMSEventType and a template case (~:122): `case 'new_lead_rancher': return \`New BuyHalfCow buyer: ${vars.buyerFirstName}${vars.state ? ' ('+vars.state+')' : ''}${vars.cut ? ' — '+vars.cut : ''}. Contact info in your email + dashboard: buyhalfcow.com/rancher\``. 2) In app/api/matching/suggest/route.ts inside the `if (introSendOk)` block (:1429), after the push: `const rancherPhone = String(topMatch['Phone'] || '').trim(); fireRancherSMSEvent({ type: 'new_lead_rancher', phone: rancherPhone, vars: { buyerFirstName, state: buyerState, cut: orderType } })` in its own try/catch (ENABLE_SMS gate lives inside fireRancherSMSEvent — dark-safe). 3) Mirror in lib/bulkRoute.ts next to its rancher email (~:325).

### 6. [in-flight (committed in wave C)] Full deposit refund silently vanishes the deal from the dashboard AND never tells the rancher
**Effort:** small · **Files:** lib/contracts/payments.ts
**Why:** restoreReferralAfterRefund flips to Status='Refunded', nulls Deposit Paid At/Rancher Accepted At/Closed At, and alerts ONLY admin Telegram (payments.ts:860-867) — while the refund physically debits the rancher's Connect balance (reverse_transfer / on-account refund). 'Refunded' matches NO dashboard bucket (activeRefs :1631, closedRefs :1632, awaitingPayment :1714, collectBalance :1727) and statusStyles has no entry (:216), so the deal evaporates; nulling the stamps even retro-deletes the activity-feed events. A rancher holding a butcher date for that buyer keeps working a dead deal while earnings silently shrink. NRD-override case included (Slot Locked is restorable).
**Fix:** 1) In lib/contracts/payments.ts restoreReferralAfterRefund after the referral flip (~:780): load the rancher (rancherId in hand at :768-769), send email via new whitelisted templateName 'deposit_refunded_rancher' — buyer name, refunded amount ((payment['Refunded Amount Cents']||capturedCents)/100), plain note that funds were returned from their Stripe balance and the deal is closed, link /rancher?tab=referrals; plus sendRancherPush({ title: `${buyerFirst}'s deposit was refunded`, body: 'the deal was cancelled — slot freed. tap for details.', url: '/rancher#deals' }). Best-effort, one-shot (gated by the existing Status==='Refunded' idempotency). 2) In app/rancher/page.tsx add 'Refunded': 'bg-dust/20 text-saddle' to statusStyles (:216) and include 'Refunded' in the closedRefs filter (:1632) so the deal stays visible with an honest badge. 3) Optional: add a refunded_at EVENT_DEF in lib/rancherCrm.ts so the activity feed records the event instead of losing history.

### 7. [in-flight (committed in wave C)] A new paid order has zero salience inside the dashboard — no badge, no Home card
**Effort:** small · **Files:** app/rancher/page.tsx
**Why:** /api/rancher/orders has exactly one consumer (ProductsTab :136), which mounts only when the rancher clicks the Products tab. Spine shows counts for Deals/Customers but Products is bare; Home composes referrals/payouts, never orders; the activity bell derives only from referral timestamps. Only signals are best-effort email (.catch(()=>{}) — and you've already had a dead-RESEND-key incident) and opt-in push. Same-day ship silently degrades to the day-3 SLA nudge on the same possibly-dead channel.
**Fix:** On dashboard mount (alongside referral/payout fetches in app/rancher/page.tsx ~:1761), fetch /api/rancher/orders (or add GET ?countOnly=1 returning { newCount }), store newOrderCount, then: (1) badge the spine: `label: \`Products${newOrderCount > 0 ? \` (${newOrderCount} to ship)\` : ''}\``; (2) Home triage card when newOrderCount > 0: '{n} paid order(s) waiting to ship' with a button calling setActiveTab('products'). Refresh the count when ProductsTab markShipped succeeds (lift count into page state or refetch on tab change).

### 8. [in-flight (wave C item 10)] Fulfillment-SLA cron false-fires on deposit-style and pickup orders — contradicts 'do not ship yet' and escalates every 48h forever
**Effort:** small · **Files:** lib/productFulfillmentSla.ts
**Why:** slaDecisions (lib/productFulfillmentSla.ts:42-51) selects purely on Status='New' + age; the cron never reads Order Ref, whose 'DEPOSIT — '/'PICKUP — ' prefixes are the only markers. Deposit orders are SUPPOSED to sit in New (settle email says 'do not ship yet — confirm size + settle the balance'), pickups sit until the buyer drives out (routinely >6d). Day 3: rancher gets '⏰ waiting to ship'; day 6+: LOUD 'refund the buyer — chargeback forming' operator Telegram every 48h indefinitely for healthy orders. Trains both sides to ignore the one alarm that matters.
**Fix:** Extend SlaOrder with orderRef and derive `kind: 'ship'|'deposit'|'pickup'` from the ref prefixes (use .includes — 'DEPOSIT — PICKUP — ' compounds; same parsing as app/api/rancher/orders/route.ts:55). In slaDecisions: 'ship' keeps current windows; 'deposit'/'pickup' get nudge 7d / escalate 14d with kind-specific copy — deposit nudge = 'reach out to confirm size + balance' (never 'ship'), pickup = 'set the pickup time'; escalation says 'stalled deposit/pickup', not 'chargeback forming'. Map orderRef in the cron's row mapping (app/api/cron/product-fulfillment-sla/route.ts:28-37) and branch templates on decision.kind. Add deposit/pickup cases to lib/productFulfillmentSla.test.ts.

### 9. [in-flight (wave C item 11)] Pickup orders render as ship orders — 'mark shipped' sends the buyer a false 'just shipped + tracking' email; the promised 'mark complete' affordance doesn't exist
**Effort:** small · **Files:** app/api/rancher/orders/route.ts
**Why:** Settlement stamps 'PICKUP — ' and the rancher email promises 'mark the order complete... once they've picked it up', but toClientOrder (orders/route.ts:55) parses only the DEPOSIT prefix. The rancher follows instructions, clicks 'mark shipped →', and the buyer who already drove home with the beef gets 'your X just shipped from Y... you'll get tracking' — reads like a duplicate order. Rancher-side success copy then falsely claims 'the buyer just got the tracking email'.
**Fix:** Add `pickup: ref.includes('PICKUP — ')` to toClientOrder in app/api/rancher/orders/route.ts:55. In POST: when pickup, ignore trackingNumber and send a pickup-complete variant ('hope the pickup went smooth — your order with {ranch} is complete') or skip the email. In ProductsTab: render a 'PICKUP' chip (like the deposit chip :823-827), hide the tracking input, relabel the button 'mark picked up →', success copy 'marked complete.' (also drop the 'buyer just got the tracking email' claim — it's false whenever the send fails or buyer email is blank).

### 10. [in-flight (wave C item 6)] Chargeback on the rancher's own connected account: rancher never notified, only Ben — evidence window burns while Telegram relays manually
**Effort:** small · **Files:** app/api/webhooks/stripe-connect/route.ts
**Why:** tier_v2 deposits/final invoices are direct charges — a dispute withdraws funds from the RANCHER's balance, and the winning evidence (tracking, photos, texts) lives with the rancher. handleDispute (stripe-connect/route.ts:792-853) stamps the Payments row + loud ops signal only; no rancher email/push at created, funds_withdrawn, or closed. Express-platform setup means Stripe won't reliably tell them either; crons that read Dispute Status only SUPPRESS rancher pings. Rancher discovers a 4-figure debit on a bank statement.
**Fix:** In handleDispute, mirror handlePayoutFailed's rancher lookup (getFirstRecord by {Stripe Connect Account Id} = event.account, pattern at :865-936 in the same file). On 'charge.dispute.created': email via new whitelisted templateName 'sendRancherDispute' — subject 'a buyer disputed a charge — respond by {evidence_due}', body: amount, buyer name (Payments row → Referral → Buyer Name when paymentRecordId found), dispute.evidence_details.due_by, and 'reply here with proof of fulfillment (tracking, photos, texts) and we'll submit it'; plus sendRancherPush({ title: `⚠️ $${amount} chargeback — evidence due ${dueDate}`, url: '/rancher#deals' }). On 'closed': short won/lost outcome email. Dedupe key = dispute.id + event type (matches the operator-signal dedupe). All best-effort after the row stamp.

### 11. [in-flight (wave C item 7)] Final tier-subscription cancellation is silent to BOTH sides — Tier wiped to None with no rancher email and no ops alert
**Effort:** small · **Files:** app/api/webhooks/stripe/route.ts
**Why:** handleTierSubscriptionDeleted (stripe/route.ts:1675-1701) sets Subscription Status='canceled', Tier='None', clears the sub id, console.logs, and returns — no email, no push, no Telegram, not even audit-log (brand/founder branches have more). Meanwhile the consequences enforce instantly: routing excludes canceled (rancherEligibility :93), deposits block (checkout/deposit :191). #376's payment-failed email fires mid-dunning and literally promises it 'keeps your plan from lapsing' — the terminal lapse then lands silently. Churn completes invisibly.
**Fix:** In handleTierSubscriptionDeleted after the updateRecord: (a) sendOperatorSignal({ urgency: 'loud', kind: 'sale', summary: `TIER SUB CANCELLED — ${ranchName}`, dedupeKey: `tier-sub-deleted:${sub.id}` }); (b) if rancher Email present, sendEmail via new whitelisted templateName 'sendRancherSubscriptionEnded': 'your BuyHalfCow plan has ended' + concrete consequences (routing paused, deposits off) + CTA `${SITE_URL}/rancher/billing` to reactivate; (c) sendRancherPush(matches[0].id, { title: 'your BHC plan ended', body: 'tap to reactivate — takes 1 minute.', url: '/rancher/billing' }). All best-effort after the row write.

### 12. [in-flight (wave C item 8)] Buyer thread messages reach the rancher via one suppressible email — no push, suppression only console.warn'd
**Effort:** small · **Files:** app/api/threads/[id]/message/route.ts
**Why:** POST /api/threads/[id]/message sends only the email mirror; if the rancher's address is bounced/unsubscribed, guardedSend suppresses without throwing and the route console.warns (:219-223) — no push, no operator signal, no retry. The unread badge updates only on a full /rancher page load (no polling anywhere), zero crons read THREADS. The push rail fires for routed-lead/deposit-paid/product-order but not for a live buyer literally typing at the rancher. Pre-purchase questions die, then show up later as 'buyer ghosted' passes.
**Fix:** In the POST handler after postMessage (~:181), when otherKind === 'rancher': best-effort `const { sendRancherPush } = await import('@/lib/rancherPush'); await sendRancherPush(recipientId, { title: 'new message from your buyer', body: messageBody.slice(0, 120), url: '/rancher/inbox' });` (dark-safe pattern from lib/rancherNotify.ts:111-122). When the mirror is suppressed for a rancher recipient, escalate: `sendOperatorSignal({ urgency: 'normal', kind: 'system-error', summary: 'Thread ' + id + ': rancher email suppressed — buyer message undelivered', dedupeKey: 'thread-mirror-' + id, dedupeWindowMs: 86400000 })` so Ben can relay.

### 13. [in-flight (wave C item 12)] Stale-qualification 412s re-fire the 'still looking?' email up to 3x/week forever — the email itself promises 'no more nags'
**Effort:** small · **Files:** app/api/matching/suggest/route.ts
**Why:** suggest's stale branch (:197-220) fires sendStillLookingReconfirm with zero dedupe — no 'Reconfirm Sent At' field exists anywhere. batch-approve re-pulls the same stale buyers daily (isQualifiedForRouting never checks freshness), each attempt burning a retry slot + firing the identical email (not whitelisted, so the only brake is the generic 3/week cap — a global count, not a per-template dedupe). Copy contradiction verified: 'no more nags about it' (emailMinimal.ts:283). Burns sender reputation on the cohort most likely to spam-report.
**Fix:** Two layers: 1) In suggest's stale branch (~:199): read buyerRecForGate['Reconfirm Sent At']; skip the send (still 412) if within 7 days, else stamp `updateRecord(TABLES.CONSUMERS, buyerId, { 'Reconfirm Sent At': new Date().toISOString() })` BEFORE sending (stamp-first house pattern). Create the 'Reconfirm Sent At' date field on Consumers. 2) In lib/qualification.ts isQualifiedForRouting, after the Qualified At check: return `{ ok: false, reason: 'stale-qualification (>28d) — awaiting reconfirm click' }` when !isQualificationFresh(buyer['Qualified At'], Date.now()) — batch-approve's skipReasonBreakdown then surfaces the cohort instead of hammering suggest daily (the buyer's reconfirm click re-stamps Qualified At, reopening the gate).

### 14. [DONE in Wave B #383] onboarding-stuck cron: stamp-less ranchers silently skipped forever + day-14 Telegram escalation loops every 4 days with no terminal state
**Effort:** small · **Files:** app/api/cron/onboarding-stuck/route.ts
**Why:** call-complete (:146) and docs-sent (:150) anchor on a single timestamp with NO fallback (the connect buckets deliberately carry 4-deep chains) — a rancher whose status Ben set by hand in Airtable without the stamp gets days=null → continue → invisible to ALL rails (email-sequences and rancher-followup have the same hole; send-onboarding's failure path even instructs hand-setting 'Docs Sent' without the timestamp). 'Call Scheduled' has zero rancher-facing chase anywhere (Left Hand MT: 3.5 months). And past day 14 the cron re-Telegrams the SAME ranchers every 4 days forever (no rancher email — the day-14 'final nudge' copy is dead code), eating MAX_PER_RUN=25 slots and training ops to ignore the channel.
**Fix:** In app/api/cron/onboarding-stuck/route.ts: (1) fallback chains — call-complete anchor = r['Call Completed At'] || r['Docs Sent At'] || r._createdTime; docs-sent = r['Docs Sent At'] || r['Call Completed At'] || r._createdTime. (2) Add a 'call-scheduled' bucket (Onboarding Status='Call Scheduled', anchor _createdTime, copy: 'Your onboarding call never happened — grab a new time' + Cal link). (3) Terminal escalation: at day14 stamp 'Onboarding Escalated At' (or encode `escalated:<bucket>` in Last Onboarding Nudge note); skip records where set unless bucketLabel changed — one escalation per rancher per bucket, then fold survivors into the Monday rancher-followup digest.

### 15. [in-flight (wave C item 13)] Self-serve closed account renders as merely 'Paused' and can one-click re-activate routing
**Effort:** small · **Files:** app/api/rancher/landing-page/route.ts
**Why:** Closure sets Verification Status='Removed' + Active Status='Paused' (remove/route.ts:96-100); magic-link verify blocks only Non-Compliant, so the closed rancher logs back in, sees the ordinary amber Paused banner (indistinguishable from vacation pause), clicks Resume — validatePauseTransition reads only Active Status and 'Paused' is an allowed source — and routing + launch-warmup buyer YES-emails turn back on for a closed account whose public page 404s (Public Map Hidden stays true). lib/pauseStatus.ts's own comments claim this is blocked; remove's write pattern defeats it.
**Fix:** (1) In app/api/rancher/landing-page/route.ts pause branch (~:413): after loading the rancher, `if (String(rancher?.['Verification Status']?.name || rancher?.['Verification Status'] || '') === 'Removed') return 403 { error: 'This account was closed. Email hello@buyhalfcow.com to reopen it.' }`. (2) In app/api/rancher/dashboard/route.ts surface `accountClosed: verificationStatus === 'Removed'`; in app/rancher/page.tsx replace the Paused banner/Resume toggle (~:1907-1945, :2244) with a distinct 'Your account is closed' card (no Resume; reopen = mailto). (3) Defense in depth: add `if (readEnumOrString(rancher['Verification Status']) === 'Removed') return false;` to isRancherOperationalForBuyers (lib/rancherEligibility.ts:80). Unit-test the rejection.

### 16. [in-flight (wave C item 14)] No session re-validation for 30 days — removing a Team Email, Non-Compliant, or closure never cuts existing access
**Effort:** small · **Files:** lib/rancherAuth.ts
**Why:** resolveRancherSession (lib/rancherAuth.ts:55-79) only verifies the JWT; status checks exist solely at login-time. Team Emails is rancher-editable and advertised for 'spouse, hired help' — but deleting one does nothing to that person's live 30-day cookie: a fired ranch hand keeps owner-privilege access (buyer PII, earnings CSV, request-deposit, Email/Team Emails edits that can lock the owner out, self-serve closure). Same hole bypasses admin Non-Compliant marks. Zero of the 32 /api/rancher/* routes re-check; no revocation mechanism exists short of rotating JWT_SECRET platform-wide.
**Fix:** Add `export async function revalidateRancherSession(session, rancher): NextResponse | null` to lib/rancherAuth.ts: return 403 + cookieStore.delete('bhc-rancher-auth') when Active Status==='Non-Compliant' or Verification Status==='Removed'; return 401 + cookie delete when session.email (lowercased) matches neither rancher['Email'] nor any entry in rancher['Team Emails'] (split on /[\s,;\n]+/, same as login) — skip the membership check when session.impersonatedBy is set. Call it in the routes that already fetch the record: /api/rancher/dashboard, /api/rancher/landing-page PATCH, /api/rancher/inbox, /api/rancher/earnings/export, /api/rancher/remove. Turns revocation latency from 30 days into one page load with zero added Airtable reads.

### 17. [DONE in Wave B #383] Expired 60-day setup link dead-ends at 'email ben@' — add self-serve re-mint (the agreement page already solved this exact problem)
**Effort:** small · **Files:** app/rancher/setup/RancherSetupWizard.tsx
**Why:** The wizard's expired-token card (RancherSetupWizard.tsx:955-967) says 'email ben@buyhalfcow.com for a fresh one' — a founder round-trip at $13k runway. /api/rancher/setup auths on the query token only, no session fallback; the only re-mint paths are admin tools and crons that stop emailing at day 14 — so your bucket-E ranchers stuck 1.5-4 months hold ONLY dead links. The e-sign page already has the fix pattern: public no-login /api/ranchers/resend-agreement. The error card also never mentions /rancher/login (which works for signed ranchers).
**Fix:** Add POST /api/rancher/setup/resend-link cloned from app/api/ranchers/resend-agreement/route.ts: body { email }; rate-limit per-email 3/15m + per-IP (reuse lib/rateLimit like /api/auth/rancher/login); match Ranchers on Email + Team Emails (trimmed/lowercased, most-recently-active tiebreak — extract findRancherByEmail from password-login into lib); require pre-live (Page Live falsy AND Verification Status != Removed); mint jwt { type: 'rancher-setup', rancherId } 60d and email the /rancher/setup?token= link via lib/email.ts. Always respond { success: true } (no enumeration). In the wizard error card (:952-966): inline email input + 'Email me a fresh setup link' button posting to it, plus a secondary link to /rancher/login for already-signed ranchers; keep the mailto as last resort.

### 18. [DONE in Wave B #383] Stripe refresh_url GET dead-ends at the login wall while a valid 60-day wizardToken sits ignored in the query string
**Effort:** small · **Files:** app/api/rancher/connect/start/route.ts
**Why:** connect/start GET (route.ts:207-235) parses from=wizard&wizardToken but auth is cookie-only requireRancher; on a different device or cleared cookie the mid-KYC rancher hits 401 → /rancher/login?next=/rancher/billing — and the login page ignores `next` entirely (router.push('/rancher')). The same rancher-setup JWT is fully trusted at setup/route.ts:94-102 and even mints the session cookie there. Admin migration tools text raw 24h Stripe URLs to ranchers, making the cookie-less cross-device hit concrete. The resume link they clicked is burned.
**Fix:** In app/api/rancher/connect/start/route.ts GET (~:229): when requireRancher fails, verify options.wizardToken as { type: 'rancher-setup', rancherId } via JWT_SECRET; if valid, proceed with that rancherId as the session AND set the bhc-rancher-auth cookie exactly like setup GET :180-208 so downstream calls work. Additionally, when from=wizard and auth still fails, redirect to `/rancher/setup?token=<wizardToken>` instead of /rancher/billing so a re-authed rancher resumes the wizard, not billing.

### 19. [DONE in Wave B #383] Wizard progress is keyed to the token string — every nudge email re-mints a token and restarts unsigned ranchers at Step 0
**Effort:** small · **Files:** app/rancher/setup/RancherSetupWizard.tsx
**Why:** stepStorageKey hashes the FULL setup JWT (RancherSetupWizard.tsx:463-473) and every re-engagement path mints a fresh JWT (onboarding-stuck, resend-setup, drips, migration-deadline) — so an unsigned rancher at Step 7 who clicks a day-7 nudge restores nothing and re-clicks through the intro. Nothing server-side computes a starting step for unsigned ranchers. (Signed ranchers are immune — server derives Step 7/9.) Bonus bug found in verification: the selfServeChosen key uses token.slice(0,32) = the constant JWT header prefix, so it's GLOBALLY shared across all ranchers on a shared browser.
**Fix:** After the rancher record loads in RancherSetupWizard.tsx, key BOTH localStorage entries on the stable record id: `bhc_setup_step_${rancher.id}` and `bhc-selfserve-${rancher.id}` (rancher id is returned by GET /api/rancher/setup); migrate by copying the old token-hash key forward on load if present. Belt: in the restore effect, when no saved step exists, derive a server-informed start — onboardingStatus in {Call Complete, Docs Sent} && contact fields present → step 1 — so a fresh-token open never re-shows the intro to a mid-flow rancher. This also fixes the cross-rancher selfServe bleed.

### 20. [in-flight (committed in wave C)] Product-order revenue is invisible in the Earnings tab and the tax CSV — dashboard and Stripe flatly disagree for product-heavy ranchers
**Effort:** medium · **Files:** app/api/rancher/earnings/export/route.ts
**Why:** Every earnings surface (Total Revenue / Your Net cards, completed-sales table, 'Sales all-time', bookkeeping CSV) is built exclusively from Closed Won Referrals; Rancher Orders money exists only as per-order lines inside the Products tab. Deposit-style product orders never create a Referral, so the money can NEVER reach earnings later. A jerky/box-heavy rancher opens Earnings, sees 'No completed sales yet', and downloads an empty 'export for taxes' CSV while Stripe shows real payouts — the tax export is materially incomplete.
**Fix:** 1) In app/api/rancher/earnings/export/route.ts: additionally load Rancher Orders scoped to the rancher (same filter as /api/rancher/orders GET: {Rancher Record ID} = session.rancherId), exclude Status==='Refunded', map to EarningsRow shape: id=Order Ref, buyerName=Buyer Name, orderType=Product Name, saleAmount=Buyer Paid, commissionDue=Buyer Paid − Rancher Payout, closedAt=Ordered At. Add a leading 'Type' column ('share' | 'product') to EARNINGS_CSV_HEADERS in lib/earningsCsv.ts (extend EarningsRow with kind?: string defaulting 'share' so existing tests only need the new column); product rows pass payout directly via a new optional netOverride consumed in buildEarningsCsv before the referralRail branch. 2) In app/rancher/page.tsx Earnings tab (~:3268): fetch /api/rancher/orders, add a 'Product sales' stat card = sum of order.payout for non-Refunded orders, and include it in 'Sales all-time' with a shares/products two-line breakdown.

