# BHC — consolidated handoff, 2026-07-19 (+07-20 → 07-25 addenda)

---

## 🚜 ADDENDUM 2026-07-25 — SUPPLY FUNNEL measured + onboarding fixed (#480-#483)

Ben: "make sure onboarding is actually as easy as we built it… so we can onboard
supply fast." Measured the real funnel (live Airtable + live Stripe reads on all
12 Connect accounts) and fixed what it exposed. All 4 PRs merged; combined main
tsc clean, **1807 tests**, build green.

**The funnel:** 87 records → 56 call done → **22 signed** → 12 Connect started →
**6 Connect active** (8 routable; legacy is exempt from the Connect gate). Two
cliffs: call→signature, and Connect 12→6.

**THE finding — the wall is Stripe SCREEN ONE, not the SSN.** All 5 stalled
accounts died identically at `tos_acceptance.date: null` with the same 5
`past_due` fields; none reached bank/SSN. Foodstead cleared the same flow in
**203 seconds**. A hand-off defect, not KYC depth — which is why Ben's call
(keep Connect REQUIRED, make it honest) is right.

**Shipped:**
- **#480** — prefill (rancher now faces **2 required Stripe fields instead of 5**);
  `Connect Started At` stamped (was **0/87** — write was unreachable, so
  abandonment was invisible); "looks live, receives nothing" detector (3 ranchers,
  incl. 5 Bar Beef blocking **281 CA buyers**); resume link + honest status.
  Latent bug also fixed: **`currentlyDueCount` was 0 for every account, always**
  (read a key the V2 Connect API doesn't define).
- **#481** — phone truncation (mangled leading-1 numbers AND forked duplicate
  rancher records), `/partner` 409 dead-end, backward resume, and the **wrong
  money model on the signature screen** + 4 live emails + `/sell`.
- **#482 SECURITY** — `/apply` handed a 60-day setup JWT on a **ranch+state**
  match (public info) = rancher account takeover; the same match also
  **overwrote the record's primary Email**, where magic-link login mails its
  token. Now only an exact primary-email match returns a token; all else emails
  the link to the on-file address with a constant response.
- **#483** — step 9 tells the truth (4 states, one action each, re-check button)
  and **refuses to advance to Sign unless Connect is genuinely active** (fails
  CLOSED on an unread status), killing the 11→6 loop.

**⚠️ OPEN — Ben's legal call:** `app/rancher/sign-agreement/page.tsx` and
`app/terms/page.tsx` still describe the DEPRECATED monthly-invoice commission
model on documents ranchers legally sign. Contract language needs Ben/lawyer.

**Also true:** AZ(129 waiting)/OH/IN/NJ/NY/MA have **zero rancher records** — a
sourcing gap no funnel fix touches. Auto-pause has no auto-unpause
(`lib/connectResync.ts:48`); Ben handles previously-paused ranchers himself.

---

## 🛡️ ADDENDUM 2026-07-24 — Shopify/product/fulfillment machine BULLETPROOFED

Ben: "make the sign-up→list→sell→fulfill machine flawless." Ran an 8-lens
adversarial find→verify audit (40 agents) → **25 confirmed defects** (6 false
alarms killed). Machine was fund-correct on the happy path but had 3 HIGH
silent goods-loss defects + 6 MEDIUM. Fixed across **11 PRs, all merged** (#468 + batches A–H + #478), each TDD +
full gauntlet, each diff reviewed (review caught + fixed one real regression:
batch A's dedup on non-unique Order Ref → repeat-buyer under-ship). Then a
**cross-batch adversarial verification (5 composed invariants)** found 2 more
HIGH holes only visible when batches interact — refund-guard fail-OPEN (ships a
refunded box on a transient read) + oversell floor excluding in-flight/pickup
sales — both fixed (#478) and **re-verified: all 5 invariants HOLD** (no
double-ship, refund-never-ships, inventory-coherence, no-new-silent-failure,
HMAC/tenant-scope). Final combined main: tsc clean, **1707 tests**, next build
green.

**3 HIGH blockers fixed:** H1 duplicate Shopify order on push retry (unique
`BHC-oid:<rowid>` tag + Idempotency-Key + 'pushing' pre-stamp); H2 refunded
order still ships (post-push Status re-read → cancel + blank-id cancel stamp);
H3 whole-beef shares leaking into the one-click rail (share-fence now delists
already-live rows).
**MEDIUM:** aged-order stranding + false-success cron (M1), dead retry
instruction → `Push Retry Requested At` field + window-independent cron (M2),
sync lock finally-release (M3), sync Orders-Left re-inflation oversell (M4),
no approval ping → supply stalls (M5), rancher UI fighting curation gate + a
server-side 409 fence closing a direct-API curation bypass (M6).
Plus B1/B2/B3 (#468: token-key fail-loud, pushed-stuck 48h health,
orders/create real-time inventory) and 15 LOW observability/security items.

**2 new Airtable fields added** (Rancher Orders): `Stock Restored At`,
`Push Retry Requested At`.
**Still Ben's:** confirm `INTEGRATION_TOKEN_KEY` set in prod (Sensitive =
pulls blank; needs a visual check) — else pushes fail (now LOUD, not silent).

---

## ⭐ ADDENDUM 2026-07-24 — MONEY MODEL LOCKED (PR #465, merged aa28d79)

Ben decided + locked the canonical money model: **buyer pays on top.** BHC's
10% is a marketplace **service fee added to the buyer's price**; the rancher
keeps **100%** of their listed price; collected via Connect `application_fee`
at deposit (final invoice fee=0); net-your-number absorbs the Stripe fee.
**Ground truth = `docs/BUSINESS-MODEL.md` → "⭐ GROUND TRUTH".** Fixed every
contradicting live surface (wizard perk, faq, /start hero, lib/tiers.ts,
StripeConnectStep, manychat script). **COPY RULE forever:** rancher-facing =
"keep 100%, buyer pays our 10% on top" — NEVER "deduct / keep 90% / minus
commission." tsc + 1575 tests + build green.

---

## ⚡⚡ ADDENDUM 2026-07-24 — ONE-ROAD onboarding rebuild (PR #464 branch)

Ben watched a rancher run the wizard: "godawful confusing." Root finding:
BOTH money rails already exist and BOTH take the fee ON TOP (shares:
`lib/stripeConnect.ts:334`; store: `lib/shopifyCatalogSync.ts:18` — synced
catalog lists at base × (1+markup%)). Both settle to the rancher's Connect
account → **Connect is the one road; the only optionality is catalog + who
ships.** None of it was ever SAID to a rancher. Rebuild:

- **New flow** (single source of truth `lib/onboardingFlow.ts`, 22 tests):
  `0 intro → 1 contact → 2 brand → 3 what-you-sell → 8 fulfillment →
  9 connect bank → 5 sign(&go live) → 6 done`. Step 4 (call) optional side
  path; step 7 (plan pick) OFF the road — reachable only via `?tier=` deep
  links. Video embed deleted. Step numbers preserved (localStorage resume);
  `remapSavedStep` handles old saved values (7→3).
- **Free-plan auto-select** on the way out of step 3 (same POST the old
  step-7 free card fired → flips Pricing Model to tier_v2 so Connect
  renders). Guarded by `shouldAutoSelectFreeTier` — fails CLOSED on
  live/signed/Page-Live/Onboarding-Live ranchers in BOTH field shapes (raw +
  camelCase; the wizard GET has NO 'Active Status'). Live legacy ranchers can
  never be silently converted (that bug shipped once).
- **Degradation**: tier/select 503 (STRIPE_CONNECT_ENABLED off) → rancher
  stays legacy, road continues, legacy auto-advances through 9 — verified in
  demo walk.
- **Store-sync in onboarding**: ShopifyConnectCard embedded in step 3 (rides
  the session cookie the wizard GET mints — zero auth changes) + the card now
  has a **marketplace margin input** (API always accepted markupPercent; card
  never sent it → synced catalogs listed at rancher cost = BHC earned $0.
  Leak plugged; onboardingPaths already refuses go-live-ready on null/0
  markup).
- **Payment-link guard** relaxed ONLY for will-auto-select applicants
  (they're about to be tier_v2); staying-legacy ranchers still required.
- **BUG FOUND+FIXED in passing**: `npm run dev:demo` silently read PROD
  Airtable server-side — `.env.local` (vercel env pull) carries
  `VERCEL_ENV=production`, tripping demo's production hard-lock on the server
  while the client banner said demo. Script now blanks VERCEL_ENV. Demo store
  gained a FRESH applicant fixture (`recDEMOrancher02x`) so the whole road
  can be walked/demoed.
- **Adversarial review (4-lens) caught 6 real bugs, all FIXED + re-verified**:
  (1) signed-legacy ?tier= upgrade links dead-ended at the "all set" screen →
  early-return now yields to ?tier=; (2/3) the degradation strand — waiving
  the payment-link requirement on a PREDICTED tier flip left a rancher
  signed-but-dark when the flip failed → advance/guard now decided on the
  REFETCHED Pricing Model, blocks at pricing with a recovery message
  (verified live in demo: 503 + no link → held; + link → advances); (4) demo
  fixture broke a pinned count test → re-pinned by id; (5) old-flow ranchers
  mid-flight at step 9 skipped fulfillment → remapSavedStep(9)→8 +
  fulfillmentDone gate on every post-Connect hop; (6) step-0 roadmap read raw
  field names absent from the wizard payload → lib/onboardingPaths now reads
  raw AND camelCase. Plus 2 self-introduced minors (markup multi-dot NaN,
  post-connect margin banner flash).
- **Verified**: tsc clean · 1575 tests (46 new across onboardingFlow +
  onboardingPaths) · real build exit 0 · full browser walk in demo mode
  (0→1→2→3→8→9auto→5, progress bar correct, sign screen reached, degradation
  block + recovery both proven; signing itself not exercised — fires real
  Resend sends).
- NOT changed: /api/apply legacy default (tier_v2-at-door killed 73% of cold
  signups — the flip now happens mid-wizard, guarded), sign-agreement,
  activate, matching.

---

## ⚡⚡ ADDENDUM 2026-07-23 — EXCLUSIVE supplier gate (Thomas Cattle) + dark ZIP campaign

Ben signed an **EXCLUSIVE** Houston/Austin supplier: **Thomas Cattle & Catering**
(Ranchers `recfvE5EgBS3WYl0r`, slug `thomas-cattle-catering`, Anthony Thomas,
Fulshear TX). A buyer outside his service ZIPs must **never** route to him.
He is deliberately **NON-ROUTABLE now** (Active Status unset, Public Map Hidden)
and stays so until Ben flips him live.

New Airtable field used: Ranchers **`Service ZIP Prefixes`** (`fld79GP7ic9YW55Bo`,
comma-separated prefixes a rancher EXCLUSIVELY serves; Thomas = `77`; add `78`
only if Ben confirms Austin). **Empty = no restriction** (every other rancher
unaffected).

### Shipped — [#462](https://github.com/benjibushes/bhc/pull/462) exclusive-ZIP gate (merged)
- **Hard gate** `lib/exclusiveZip.ts` (`buyerZipServedBy`, 13 unit tests): a
  ZIP-gated rancher is eligible ONLY if the buyer's ZIP starts with a prefix.
  **FAILS CLOSED** — no/malformed/out-of-prefix ZIP → excluded. Wired into
  `matching/suggest` `isEligibleBase` (covers local + nationwide) AND the
  **direct-pin path** (the one route `isEligibleBase` never guarded — an
  out-of-Houston buyer clicking Thomas's page would otherwise direct-match him).
- **Exclusivity:** in-zone exclusive rancher sorts FIRST among eligible (new
  step `-1` in the local comparator) → Thomas gets ALL qualifying Houston buyers.
- **Close-queue gate:** `/api/admin/send-deposit-invoice` returns **422** for a
  ZIP-gated rancher unless the buyer ZIP is confirmed in-prefix (money step
  behind the routing gate).
- **Branded emails:** "meet your rancher" intro (`lib/email.ts`) + deposit
  invoice (`lib/emailMinimal.ts`) now render the rancher's **Tagline + About
  Text** (optional → blank fields drop the block). Wired from `matching/suggest`
  + both deposit rails.

### Shipped DARK — [PR2] ZIP-gathering campaign (env-gated, dry-run default)
For the ~248 legacy TX WAITING buyers with **no ZIP** (routing can't place them).
**Nothing sends** until Ben flips env flags — see `docs/ZIP-GATHER-CAMPAIGN.md`.
- `lib/areaCodeMetro.ts` (8 tests) — phone area-code → Houston/Austin metro.
- `lib/zipGatherCampaign.ts` (11 tests) — audience select (TX/WAITING/no-ZIP/
  metro-phone/emailable/not-suppressed) + TCPA/CAN-SPAM copy.
- `app/api/cron/zip-gather` — DARK cron; `ZIP_GATHER_ENABLED` arms it,
  `ZIP_GATHER_SEND` sends (else dry-run reports audience + copy). Email live,
  SMS dry-only (no A2P sender wired).
- `app/api/zip-confirm` — one-tap signed-link capture; writes ONLY `Consumers.Zip`.

### ⚠️ Ben action to go live (do NOT skip step 1)
1. Set Thomas `Active Status=Active` + confirm `Service ZIP Prefixes` (`77`,
   add `78` only if Austin). Until then he stays non-routable (correct).
2. Dry-run: `curl -H "Authorization: Bearer $CRON_SECRET" .../api/cron/zip-gather`
   with `ZIP_GATHER_ENABLED=true` → review `audienceCount` + `byMetro`.
3. `ZIP_GATHER_SEND=true` to begin sends (capped by `ZIP_GATHER_DAILY_CAP=50`).

*Money/routing invariant added: a rancher with `Service ZIP Prefixes` set is
ZIP-exclusive on EVERY path (local, nationwide, direct-pin, admin deposit send).*

---

# BHC — consolidated handoff, 2026-07-19 (+07-20, +07-21, +07-22 addenda)

*Single source of truth. Supersedes `docs/MONEY-TRUTH-INVARIANTS.md` (its
Part 1 premise is WRONG — see Corrections). Read the 07-21 addendum first —
it closes out the entire 07-20 build order.*

---

## ⚡⚡ ADDENDUM 2026-07-21 — build order CLEARED (all 6 items)

One session, all six items from the 07-20 build order either shipped or
closed as verified-not-a-bug. Every claim below was live-probed.

### Shipped (merged to main, deploy verified)
- **[#418](https://github.com/benjibushes/bhc/pull/418)** Durable `/r/f/<token>`
  final-invoice links (item 3). `send-final-invoice` now stores + emails our
  own 30d link, never the raw ~24h Stripe URL; `/r/f` re-runs the money gates
  at click and reuses-or-re-mints a fresh Stripe session; dunning reminders
  automatically re-send the durable link. Dunning's heal-or-skip resolver now
  reads the new `Final Invoice Checkout Session Id` field (URL-parse kept for
  legacy rows). **Prod-verified: garbage token → 302 `/ranchers`.**
  → **Dana can now be re-sent safely**: rancher-dashboard resend or
  `POST /api/rancher/referrals/recOlGI4mMdSDsz9V/send-final-invoice?resend=true`
  mints a durable link. Still a Ben action (it emails the buyer).
- **[#417](https://github.com/benjibushes/bhc/pull/417)** Legacy signup default
  + self-serve-first wizard (item 5). All THREE creation doors flipped
  (`/api/apply`, self-submit, partners — lockstep contract kept). Call is now
  the secondary path; step-restore clamp removed. Companion guard: payment
  link is REQUIRED (blocks Continue) for legacy ranchers — without it the flip
  would have converted the SSN wall into a silent payment-link stall at
  `Agreement Signed`. Existing ranchers untouched (create-path only).
- **[#416](https://github.com/benjibushes/bhc/pull/416)** Deleted the 5 dead
  reads of nonexistent `Stripe Account Id` (item 4 — see corrections below).
- **[#419](https://github.com/benjibushes/bhc/pull/419)** Deposit watchdog cron
  + go-live payment-path smoke test (item 6, spec 1b/1c/1d). Watchdog
  (`25 * * * *`): Awaiting Payment + no `Deposit Invite Sent At` + >2h →
  loud Telegram, claim-stamped `Deposit Watchdog Alerted At` (24h cooldown),
  cap 10/run. Also fixed the cry-wolf gap: admin `send-deposit-invoice` now
  stamps `Deposit Invite Sent At` (it never did — only the rancher door did).
  Smoke test (`lib/paymentPathSmoke.ts`): static gates + LIVE Connect probe;
  blocks the 3 human go-live doors (activate, admin go-live w/ `?force=true`
  escape, spgolive) listing the exact failed gate; nightly canary added as
  check 15 of nightly-rancher-audit. Deliberately NOT wired: Connect-webhook
  auto-go-live + go-live-sync (Connect-active is their trigger — circular),
  legacy payment-link URL reachability (false-positive machine). 31 new tests.

### Closed as VERIFIED, no code needed
- **Item 1 — the 18 money-email suppressions ALL predate the whitelist fix.**
  Whitelist commit `2aa3ece` merged 07-17 15:28 UTC; the latest of the 18
  `cap-exceeded` suppressions is 07-17 14:45 UTC (43 min before). Post-fix:
  ZERO suppressions, nudges flowing (`sent` 07-18, 07-20, 07-21). The
  whitelist path works. Gail (gjwolfe50@) + Crystal's suppressions were 07-06
  / 07-08 — pre-fix. Nothing is capping money email today.
- **Item 4 — two of the three "wrong-field-name money reads" were audit-script
  false positives.** `Amount` IS written (object-literal key in
  `app/api/admin/ad-spend/route.ts:63` — and it's the Ad Spend table, not
  money-path). `Deposit Style` is an OPS-MANAGED field Ben checks by hand
  (validator + 409 fences deliberately exclude it — PR #290/#293). Only
  `Stripe Account Id` was real dead code (field doesn't exist on Ranchers) —
  deleted in #416. **Audit-script blind spots to fix before trusting it in
  CI: object-literal-key writes + ops-managed fields whitelist.**

### ✅ Item 2 — Resend webhook: WORKS. The 07-20 "not firing" was a phantom.
Proven live (same session, second pass after Ben pushed back on my first wrong
"not registered" diagnosis — twice wrong, both times for hard-rule-#1 reasons):
- Webhook `5a321579` registered since Apr 13, enabled, endpoint
  `buyhalfcow.com/api/webhooks/resend`, events bounced/complained/delayed.
- **End-to-end test PASSED**: sent prod email to Resend's `bounced@resend.dev`
  simulator against a throwaway Consumers row → webhook stamped
  `Bounced=true` + `Unsubscribed=true` + Notes within ~1 min. Secret matches,
  signature verifies, suppression rail is fully healthy. (Test row deleted.)
- **Nikki's bounce DID process on 07-20** — her rancher Notes carry
  `[Auto-flag 2026-07-20] Email bounced`. The "still blank" `Bounced` field
  was mis-read: the rancher branch of the webhook only wrote Notes; the
  Bounced/Complained CHECKBOXES exist on Ranchers but nothing wrote them.
  Fixed this session (see PR below): rancher branch now stamps the checkboxes
  too, and Nikki's row was backfilled `Bounced=true`.
- Also true: engagement events (`delivered/opened/clicked`) are NOT subscribed
  on the webhook, so the route's Email Sends engagement-stamping code
  (Delivered At / Opened At etc.) has never run. OPTIONAL Ben decision: enable
  those events in the Resend dashboard on webhook `5a321579` if open/click
  visibility in Airtable is wanted — code is already there waiting.
- Key hygiene: `.env.local`'s `RESEND_API_KEY` (Jun 9, 38-char) is DEAD —
  rotated out; prod uses a different valid key. Update `.env.local` + the
  Resend MCP plugin key or every local session will misdiagnose Resend.

### New Airtable fields created this session (Referrals)
- `Final Invoice Checkout Session Id` (text) — dunning resolver pointer.
- `Final Invoice Link Opened At` (dateTime) — /r/f first-open gauge, sibling
  of `Deposit Link Opened At`.
- `Deposit Watchdog Alerted At` (dateTime) — watchdog claim-stamp/cooldown.

### Corrections to prior handoff/memory numbers
- **Vercel crons: 58 registered in vercel.json, ≥38 distinct crons fired in
  the past 24h** (live Cron Runs probe). Memory's "7 crons" is badly stale.
- Handoff line "still open" on cap-suppressions: resolved (item 1 above).

### ✅ FULFILLMENT CONNECTOR — SHIPPED (same day, 2026-07-21 PM)
All code slices merged + build-gated same session the plan was approved:
- **[#424](https://github.com/benjibushes/bhc/pull/424)** PR-A: AES-GCM
  credential crypto (`INTEGRATION_TOKEN_KEY`), provider-agnostic
  `FulfillmentConnector` contract, Shopify adapter (orderCreate PAID +
  inventory decrement + Shopify-owned tracking email; orderCancel restock;
  SKU→variant; address split verified against `formatShipping`'s exact
  output). 8 Airtable fields created pre-merge (Ranchers `Fulfillment
  Integration`; Products `External SKU`/`External Product Id`/`Sync
  Managed`/`Last Synced At`; Orders `External Order Id`/`External Push
  Status`/`External Pushed At`).
- **[#425](https://github.com/benjibushes/bhc/pull/425)** PR-B: push wired
  into `settleProductPurchase` (race-guard winner only), outcome stamped on
  the order row, `fulfillment-push-net` cron (35 */2, 3-day window — also
  catches orders settled before the store was connected), FULL refund/dispute
  cancels the external order (partial leaves it live), cancel-fail rings ops.
- **[#426](https://github.com/benjibushes/bhc/pull/426)** PR-C:
  `/api/webhooks/shopify` (per-rancher HMAC) — store fulfills → Tracking
  Number + Shipped At + Status stamped; no double buyer email; SLA stops.
- **[#427](https://github.com/benjibushes/bhc/pull/427)** PR-D+E: Telegram
  `/connectstore` (live validation, webhook registration, encrypted save,
  SKU dry-run report) + `/syncstore`; catalog sync engine (Sync-Managed rows
  only, markup pricing, Active mirrors store stock) + `shopify-catalog-sync`
  cron (55 */6) + PRODUCTS_UPDATE near-real-time resync; go-live smoke +
  nightly canary gained a live shopify-auth gate. Verified:
  `ensureStripePrice` mints on first sell → synced rows sellable instantly.
- `INTEGRATION_TOKEN_KEY` set in Vercel prod (Sensitive) + `.env.local` +
  test env. Tests 1264 → 1322 across the session.
- Connector is INERT until a store is connected — zero risk to current rails.

- **[#429](https://github.com/benjibushes/bhc/pull/429)** PR-F (same day):
  rancher SELF-SERVE connect — "Connect your Shopify" card on the dashboard
  Products tab (inline custom-app walkthrough, live validation, sync/manual
  choice) + session-scoped authed endpoint; and the CURATION GATE — synced
  products import unapproved, display on /shop only after Ben checks the new
  `Marketplace Approved` checkbox (Active = approved ∧ in-stock ∧ ACTIVE;
  stock flaps automatic post-approval). Card not yet browser-verified (auth
  wall) — first render check is part of the pilot.

- **[#432](https://github.com/benjibushes/bhc/pull/432)** Phase 1 ONE-CLICK
  install rail (same day, adversarially reviewed pre-merge — 16-agent
  workflow, 8 confirmed findings fixed, 5 refuted): `/storelink` stages a
  custom-distribution app (Partner Dashboard, no App Store review) and
  returns a link; distributor clicks → Shopify consent → callback exchanges
  the code, configures the connector, registers webhooks, dry-runs the
  catalog, lands on the branded `/store-connected` page. Guards: live-config
  clobber protection (`force` required), unique rancher match, mid-flow
  failures ring Ben, honest failure copy. Partner Dashboard app config:
  App URL = site root, redirect URL = `/api/shopify/oauth/callback`.
  **Pilot should now run THROUGH this flow** (stage with /storelink for the
  ZZ PILOT rancher, click the link yourself).

- **[#434](https://github.com/benjibushes/bhc/pull/434)** Pre-pilot audit
  fixes + UI polish. 29-agent full-chain audit found + fixed: the
  'Marketplace Approved' checkbox was a 6-HOUR NO-OP (nothing recomputed
  Active) → new Telegram `/approvestore <rancher> [| SKUs]` approves + goes
  live in seconds; null-markup synced rows could NEVER display or sell
  (Display Price unwritten) → create-time zero-margin default; share-fence +
  $5 floor now enforced on sync; webhook registration failure detection
  hardened; exact DEPOSIT/PICKUP marker matching. UI: /store-connected
  redesigned to site canon + connect card matched to ProductsTab (browser-
  verified desktop+mobile).
  **Open review items — CLOSED by #436 + the GTM closeout PR** (markup AND
  category preserved on re-connect; refund-cancel loops all rows; remaining
  accepted-as-is: net-cron 20-cap under backlog, whole-catalog resync per
  products/update event — both fine at current scale).
  **Pilot demo notes:** approval is PER VARIANT row (a 3-size shirt = 3
  rows; use /approvestore all); no-SKU variants never import; merch items
  show beef-flavored copy ('ships frozen') on PDP — cosmetic, known.

- **[#437](https://github.com/benjibushes/bhc/pull/437)** Merch = its own
  /shop section ('herd merch', ordered last; beef always leads). New
  Category 'Merch' + connector config `category` (stamp via
  `/storelink ... | category:Merch`), merch-aware PDP copy (no 'ships
  frozen' on a hat). Pilot row renamed PERMANENT: **BuyHalfCow Merch**
  (rec4pnnjfp2nTaS1V) — Ben decision: merch store is the first real
  connected distributor, not a throwaway test.

- **[#439](https://github.com/benjibushes/bhc/pull/439)** PUBLIC-APP one-click
  connect (Phase 2 code) — Ben verdict: token flows aren't rancher-grade.
  Rancher flow now: dashboard card → store address → sync/manual → approve on
  Shopify → done. Env creds `SHOPIFY_APP_CLIENT_ID/SECRET`; compliance
  webhooks (customers/data_request+redact ack-only, shop/redact clears
  config, HMAC 401 on bad sig) on /api/webhooks/shopify; card auto-falls
  back to the token form until the app is approved.
  **ACTIVATION (Ben, one-time): Partner Dashboard → create public app
  'BuyHalfCow' (App URL site root, redirect /api/shopify/oauth/callback,
  compliance webhook URLs /api/webhooks/shopify) → set the two env vars in
  Vercel → submit for App Store review (~1-2 wks). Until approval the
  custom-app doors carry everyone.**

- **PUBLIC APP LIVE-CONFIGURED (2026-07-21 PM, driven in-browser):** Shopify
  app "BuyHalfCow" created in Ben's Partner org (client id
  8917d002f8f87573368160965fe1372a), version oauth-v1 Active: App URL site
  root, redirect /api/shopify/oauth/callback, scopes read_orders/
  write_orders/read_products, NON-embedded, LEGACY INSTALL FLOW ON (matches
  our authorization-code routes). SHOPIFY_APP_CLIENT_ID/SECRET set in Vercel
  prod (Ben ran the commands). **Distribution = PUBLIC (chosen, irreversible,
  Ben-authorized).** **SUBMITTED FOR APP STORE REVIEW by Ben 2026-07-21** (~1-2 wks; Shopify
  emails the verdict — treat review feedback as next-session work items).
  Compliance webhook URLs: if the submission form doesn't collect them,
  wire via app TOML next session (route already handles the 3 topics).
  One-click rancher connect goes fully live on approval; the dashboard card
  flips automatically now that env creds exist (this deploy).

**NEXT: the pilot (plan Task 10, ~30 min, needs Ben).** Merch store
(merch.buyhalfcow.com): create custom app (write_orders, read_orders,
read_products) → `/connectstore <test rancher> | shop | token | secret |
manual` → real small purchase → verify PAID order lands tagged BHC +
inventory decrements → fulfill w/ tracking in Shopify → verify BHC row flips
Shipped + exactly 1 receipt + 1 tracking email → Stripe refund → verify
auto-cancel + restock + `cancelled` stamp. All four proofs green → first
distributor conversation.

### 🔎 GTM final audit (22 confirmed findings — ALL code items fixed same day)
Full-platform 25-agent audit before market. Fixed in the closeout PR:
signed-legacy wizard re-entry trap (forced tier/SSN dead-end), welcome +
post-sign emails promising the retired tier_v2 journey, sign/done screens
claiming a verification gate that no longer exists, Products-tab closed loop
for legacy ranchers (now unlocks on Connect-active alone), connect card
context, watchdog false alarms on legacy deals (rail-scoped now), store
health no longer blocks beef go-live (warnings channel), canary transient
handling, hat-in-beef-funnel exclusion, SLA cron push-blind fix, category
survives re-connect + rides public OAuth, /approvestore warns on missing
Connect, env registry updated.
**Ben-only residue:** (1) the Commission Agreement text at
app/rancher/sign-agreement/page.tsx:247-278 still describes the OLD flow
(mandatory alignment call, verification before listing) — legal doc, needs
your wording call; (2) BuyHalfCow Merch row needs Stripe Connect before
merch can sell (approve warns you now); (3) plan-doc Task 10 runbook is
historical — follow the handoff, not the plan doc, for the pilot.

### Still Ben-side (unchanged)
- Text the 6 unpaid buyers (~$4,350) + re-send Dana ($2,249, durable now).
- Vale Creek / Nikki: find via Google/FB, fix email, no phone on file.
- Resend webhook registration (runbook above).
- $2,920 commission-collected question (§3d).

---

## ⚡⚡ ADDENDUM 2026-07-22e — NEAREST-RANCHER ROUTING by real miles (#452 + #453)

Routing was state-granular: a buyer 20 miles from a ranch and one 400 miles
from it looked identical to the sort. Both halves shipped this session.

### [#452](https://github.com/benjibushes/bhc/pull/452) — the routing side
- `lib/geoDistance.ts` — pure haversine (miles), Airtable point coercion
  (rejects the `0,0` un-geocoded sentinel), the `distanceCmp` sort fragment,
  the delivery-radius verdict.
- `lib/zipCentroids.ts` + `lib/zipCentroids.data.ts` — **42,249-ZIP offline
  centroid table**, parsed lazily (~14 ms, once per lambda). Deliberately NOT
  a live geocode: matching/suggest runs on the money path for every qualified
  buyer, and a network hop there is a latency + rate-limit failure mode during
  exactly the demand spike we want to survive. `lib/geocode.ts` stays the
  RANCHER-signup geocoder. Regenerate: `node tools/generate-zip-centroids.mjs`
  (provenance: `zipcodes` npm, MIT — vendored as a derived table, **not** a dep).
- `matching/suggest` — BOTH sort blocks now rank by actual miles;
  `hopDistance` is kept as the fallback tiebreak. Primary-state preference
  still wins first (deliberately unchanged — see "open question" below).

### ⚠️ The delivery-radius correction — read before touching this
The plan said "buyer outside `Delivery Radius Miles` → drop from local".
Reading `RancherSetupWizard.tsx:3982` changed the design: **that field is a
required sub-field of the "Local Delivery" checkbox** — it answers *"how far
will you drive an order"*, NOT *"how far will you serve a buyer"*. So the gate
fires ONLY when `Local Delivery` is the rancher's SOLE fulfillment type.
Live proof it matters (probed 07-22, the only 2 ranchers with a radius set):
- **Champion Valley Farm (NE)** — Local Delivery **+ Local Pickup**, 200 mi,
  routes CO. Denver is ~380 mi. **Never gated.** A naive gate would have
  waitlisted every Denver buyer — there is no other CO supply.
- **Gift Farms LLC (OK)** — **Local Delivery only**, 200 mi. **Gated.**
  Correctly stops taking Houston (~435 mi) leads it can't physically serve.

Out-of-radius **DEMOTES** by default (sorted last, still matchable), mirroring
the existing `ROUTING_ADJACENCY_ENFORCE` knob — a mistyped radius would
otherwise silently black-hole a rancher's whole lead flow. Flip
**`ROUTING_RADIUS_ENFORCE=strict`** to actually exclude (it logs what it
dropped). The demotion sorts ABOVE the primary-state preference: "is in your
state" is worth nothing if they can't get the beef to you.

### [#453](https://github.com/benjibushes/bhc/pull/453) — the capture side
- Optional "ZIP code" field on the funnel contact step. Blank ZIP submits and
  routes exactly as before; a HALF-typed ZIP is rejected (typo, not a choice).
- `/api/consumers` writes `Zip` on BOTH doors, gated on `normalizeZip` — blank
  or garbage writes nothing, never a 400.
- `lib/zipFormat.ts` holds `normalizeZip` with zero imports so the signup
  lambda gets a 20-line validator, not the 1 MB table.
- Landmine killed: `maxLength={5}` fights the digit-strip (browser caps RAW
  chars before onChange) — typing `78a7-0b1x9` produced `787`. Removed.

### Backward compatible, by construction and by test
No buyer ZIP / ZIP whose state disagrees with the routing state / un-geocoded
rancher / no radius ⇒ every helper returns null-or-no-gate ⇒ today's state-hop
sort governs byte-for-byte. Explicit test asserts zero reordering across the
whole live roster when the buyer has no point. The #449 Routing-States gate is
untouched and still the outer filter; quiz-required qualification untouched.

### ⚠️ CORRECTIONS to this addendum (4-lens verification, same session)
Four independent checks re-derived every number from raw Airtable (read-only).
The deploy status, the distances and the single flip all held. These did NOT:
- **"Ships inert until the funnel captures a ZIP" (in #452's commit message) is
  WRONG.** `app/api/orders/request/route.ts:209` has written `Consumers.Zip`
  from the rancher storefront order form since 2026-07-02 — unnormalized
  (`zip || ''`, no `normalizeZip`). Real-miles routing arms on the first
  storefront order carrying a ZIP, with no funnel involvement.
- **Routable supply is 7, not 11.** 11 have Active Status='Active'; 8 pass
  `isRancherOperationalForBuyers`; **Lazy Bar 3 BEEF / Texas then fails
  `passesTierV2CutFloor`** — it is `tier_v2` with Quarter/Half/Whole Price ALL
  UNSET, so the route rejects it for every buyer. **TX (337 buyers, the largest
  cohort) therefore has exactly ONE eligible rancher: Gift Farms in OK**, which
  is delivery-only/200 mi and out of range from Dallas (224), Houston (437),
  Austin, San Antonio, El Paso, Amarillo. Fix = 3 numbers in Airtable — BEN'S
  CALL (hard rule 5). Connect is already active on that row.
- **Lazy Bar's coordinates are the Texas state centroid, not a ranch.** Its row
  has no City and no Zip, so signup fell back to `lib/geocode`'s `'<state>, USA'`
  path → 31.969/-99.901 (geographic centre of TX). Set City/Zip and re-geocode
  BEFORE entering prices, or every TX distance is off by up to ~400 mi.
- **Buyer census was wrong.** Not 473/506/1,668 — the real split is
  **136 contestable (CO 110, KS 26) / 843 solo / 1,766 zero-eligible**, summing
  to 2,745. The first pass counted TX as a 2-rancher state and silently dropped
  **98 buyers with a blank State** (permanently unroutable — `normalizedBuyerState`
  drives the whole gate). With each buyer's own Order Type + Budget applied it
  tightens to 91 / 646 / 2,008.
- **"Nearest wins, period is harmless" was UNTESTED, not proven.** No state today
  has an in-state rancher competing with an out-of-state one (CO/KS pools are
  Champion Valley NE + Gift Farms OK — both non-primary), so the primary-state
  step never decided anything. It gets teeth the day Lazy Bar is priced.
- **Biggest lever in the data is not routing:** 5 Bar Beef (CA, 304 buyers),
  2M Cattle Co. (TN, 73), Rocky Ridge (ME, 8) are Active + signed + Live +
  paid, blocked ONLY by `Stripe Connect Status='onboarding'`. Finishing those
  3 flows converts **385 buyers** from zero-eligible to routable — more than
  the entire surface this routing change touches.
- **`ROUTING_RADIUS_ENFORCE` verified NOT set in Vercel prod** (nor any other
  `ROUTING_*`/`NATIONWIDE_*`), so the gate is in safe demote mode. Do NOT flip
  it to `strict`: Gift Farms is the sole eligible rancher in TX and NM and is
  out of its own radius from every metro there — strict would waitlist ~365
  buyers with a willing rancher sitting in the pool.
- Latent: the comparator is non-transitive if a pool mixes geocoded and
  un-geocoded ranchers (14 of 83 rows have no coordinates). Unreachable today
  (largest pool is 2, all Active rows geocoded). Make lat/lng a precondition
  for activating a rancher into a state that already has one.

### State of play + what's next
- **All 2,745 existing consumers have a blank Zip**, so miles-routing stays
  inert until a buyer supplies one — via the funnel (#453) OR the pre-existing
  storefront order form (see corrections). Nothing to backfill.
- 69/83 ranchers are geocoded; **all 11 Active ones are**. Un-geocoded
  ranchers simply fall back to hop-sorting.
- Verified live in the browser with `/api/consumers` fetch blocked; confirmed
  0 test rows written to the base afterward. Tests 1428 → 1477, 0 fail.
- **Open question for Ben** (deliberately NOT decided in code): today a
  primary-state rancher still beats a nearer out-of-state one. Real config
  makes this near-moot (primary and nearest coincide for every current
  pairing), but if you want "nearest wins, period", it's a one-line move of
  the distance compare above the primary-state check.
- Optional follow-up: a `Match Distance Miles` field on Referrals would let us
  PROVE the ranking in Airtable rather than in Vercel logs. Not created — new
  fields are Ben's call.

## ⚡ ADDENDUM 2026-07-22d — deposit-watchdog false alarm: root-caused + self-healed (#450)

- Watchdog Telegrammed "deposit invite NEVER SENT" on 6 referrals (Crystal,
  Brent, Meagan, Lori, Joann, Gail). **All 6 were actually invited 2-3× each**
  (Email Sends: `buyer_deposit_invoice` / `quiz_complete_deposit_invite`, status
  `sent`). Blank `Deposit Invite Sent At` only because they were invited BEFORE
  #410 added that stamp — classic hard-rule-1 (blank ≠ didn't happen).
- **Backfilled** the 6 `Deposit Invite Sent At` from Email Sends truth (ops
  write, guardrail-checked; nudge rail already at lifetime cap so no new sends).
  Watchdog target set now 0.
- **[#450](https://github.com/benjibushes/bhc/pull/450)** watchdog now
  self-heals: before alarming, it reads the buyer's `sent` deposit invites; if
  one exists it backfills the stamp + skips the alarm (nudges/suppressed don't
  count; read-fail falls through to alarm). `earliestSentDepositInvite` +5 tests.
- The 6 are stale warm leads (invited, unpaid, 3wk cold) — task chip spawned to
  personal-text them their still-live /r/p link, or Closed Lost. NOT more auto-nudges.

## ⚡⚡⚡⚡⚡ ADDENDUM 2026-07-23 — FULL-PLATFORM FLAWLESS PASS

22-agent whole-platform verification audit (adversarial + LIVE prod probe) →
**0 blockers**, 83 behaviors verified correct, prod confirmed live/synced
(8 active ranchers, 2578 buyers, 21 closed won). The 9 degraded + security/
money polish FIXED in **#460** (1497 tests green, 6 worktrees zero-conflict):
- MONEY: final-invoice /r/f now serialized (claimOnce final-mint:<ref>) —
  double-charge window on the biggest charge CLOSED; settleBuyerDeposit
  clamps Deposit Amount ≤ charged (resolveDepositCents).
- AUTH/SECURITY: magic-link bypasses suppression (no more silent login
  lockout); Telegram secret → timingSafeEqual; Resend webhook formulas →
  escapeAirtableValue; Telegram GO-LIVE button routed through goLiveRancher
  (gates + smoke, no bypass).
- CORRECTNESS: Cal webhook idempotent on every early-return; Shopify sync
  per-rancher lock (no dup SKU rows); sold-out rancher can't be force-pinned;
  community rancher-lead gets a qualify link; go-live via GO_LIVE_FIELDS incl
  Status=Active; abandoned-quiz-nudge partial-on-broken-read; 3 cron schedule
  collisions staggered; per-rancher rescue dedupeKey; whitespace-tolerant
  dedupe formula; sendOperatorPreCallBrief whitelisted.
- Cosmetic: "Foodstead " trailing space trimmed (was leaking into page title
  + public stats JSON).
- Also this session: onboarding hardening #457 (silent-signup-failure
  impossible — Signup Attempts table + beacon + rescue alerts + canary),
  ZIP-routing + email-facelift handed to builders, DB hybrid classification
  (docs/DB-HYBRID-CLASSIFICATION.md — keep Airtable for ops-UI tables, move
  append-logs+Consumers to Supabase; Phase 0 = cache Ranchers reads first).

## ⚡⚡⚡⚡ ADDENDUM 2026-07-22c — REACTIVATION READINESS: 42 found, 41 FIXED

Pre-launch audit of the WAITING-buyer reactivation pipeline (52-agent sweep +
6-worktree fix fleet) before Ben unlocks nationwide supply. **Read
`docs/NATIONWIDE-REACTIVATION-RUNBOOK.md` before flipping anything.**

- **Blockers killed:** campaign pools now Routing-States-gated (no more
  cross-state deposits); N-rancher pool model (DEMAND_CAMPAIGN_RANCHER_IDS
  takes any count, per-pool capacity + fall-through, 1-2 ids = legacy
  behavior); legacy quiz vocab ("1-3 months") maps to current timing so the
  old cohort can't be auto-held; failed sends now refund lifetime touches +
  alarm (both rails) + 3-consecutive-failure circuit breaker; capacity =
  Closed Won gate everywhere w/ next-candidate fall-through; broadcast cursor
  is identity-based (no double-sends).
- **New rails:** READY-never-matched chase (2nd stage inside
  waiting-activation, CTA = in-state rancher page); reserve-abandon recovery
  generalized beyond the campaign (RESERVE_ABANDON_RECOVERY_ENABLED).
- **Cross-rail cooldown:** waiting-activation and demand-router now respect
  each other's last-sent stamps — one buyer, one stream per window.
- **Scale:** Cron Pauses read cached 60s (pause-first semantics kept — the
  emergency stop still beats the whitelist); frequency-cap batch prime; run
  locks; maxDuration + in-loop budgets; started-heartbeat Cron Runs rows;
  shared Resend token bucket.
- **⚠️ BEN — Airtable fields to CREATE before going live** (code tolerates
  absence, rails stay dark/abort loudly until they exist):
  - Consumers: `Ready Nudge Last Sent At` (dateTime), `Ready Nudge Count` (number)
  - Referrals: `Reserve Recovery Sent At`, `Reserve Recovery SMS Sent At` (dateTime)
  - Then run the dry-run and confirm "stamp schema OK" for both pairs.
- Env knobs: `WAITING_NUDGE_MAX_PER_RUN` (0 now honored), `READY_CHASE_MAX_PER_RUN`
  (default 25), `RESERVE_ABANDON_RECOVERY_ENABLED`, schedule moved to 16:20 UTC.
- One skip (justified): campaign readBuyers formula narrowing — planner needs
  the full in-arc set for outstanding-invite counting.

## ⚡⚡⚡ ADDENDUM 2026-07-22b — onboarding audit: 36 found, 34 FIXED (#446)

- **Full-cycle rancher-onboarding audit** (43-agent adversarial sweep + live UX
  walk of /sell, /apply, /map/add-a-rancher) ahead of Shopify launch: 36
  verified findings, 34 fixed in **[#446](https://github.com/benjibushes/bhc/pull/446)**
  (4 fix groups built in isolated worktrees, hand-integrated, 1352 tests green).
- **Blockers closed:** Shopify one-click gated behind `SHOPIFY_PUBLIC_APP_LIVE`
  (⚠️ **BEN: set =1 in Vercel when Shopify approval email lands** — until then
  the card auto-falls back to the token form); self-submit dedupe now mints the
  setup JWT for existing records (was silent bounce + false success); admin
  Mark-Live runs the payment smoke; welcome/approval email whitelisted +
  loud-on-failure (wizard URL rides the Telegram alert); paused_overdue
  truth-heals + fires UNPAUSE operator signal (never auto-flips Active Status).
- Chase rails honest now: /apply dropouts get a real setup link (was yellow-pin
  lie forever); agreement reminders carry a signing CTA; post-sign emails have
  dedicated whitelisted template names.
- Shopify hardening: APP_UNINSTALLED handled (products pulled + signal),
  store-deleted SKUs deactivate after full pagination, untracked/continue-sell
  inventory sellable, /store-connected has a rancher variant.
- **REMAINING (2 findings, deferred): email design unification** — ~90
  templates, 5 looks, 0 match the money-email target; CAN-SPAM footer appended
  after </html> on ~60. Next BUILD session: lib/emailLayout.ts renderEmail()
  + wave migration (task chip spawned; money templates first; cold outreach
  stays plain text).
- Parallel sessions have branches in flight for build items 4/5/6:
  `fix/dead-stripe-account-id-reads`, `feat/legacy-default-signup`,
  `feat/deposit-watchdog-smoke` — check before duplicating.

## ⚡⚡ ADDENDUM 2026-07-22 — Dana PAID · final-invoice rail closed FLAWLESS

- **$2,249 COLLECTED — Foodstead deal Closed Won** (`recOlGI4mMdSDsz9V`,
  total $2,999). Full incident cycle, all live-verified:
  raw link #1 (Jul 19) died → Dana asked resend → raw link #2 (Jul 20) died →
  Bren Palanjian (splitting the half with Dana, brenpalanjian@gmail.com) hit
  "completed or timed out" Jul 21 → ops session minted durable `/r/f` token
  locally (JWT_SECRET local == prod), live-fired prod (302 → fresh
  `cs_live_a1B1TZ…`), emailed Dana+Bren via Resend (delivered 01:29 UTC) →
  **PAID 01:32 UTC — 4 minutes after delivery.** Webhook settled clean:
  Closed Won + Final Paid At/Amount + Sale Amount stamped.
- **[#418](https://github.com/benjibushes/bhc/pull/418)** (built by a parallel
  session) durable `/r/f/<token>` final-invoice links — re-mints live Stripe
  session at click time, 30d token, gates re-run per click, `Final Invoice
  Link Opened At` + `Final Invoice Checkout Session Id` stamped. Build-order
  item 3 = DONE.
- **[#444](https://github.com/benjibushes/bhc/pull/444)** (this session)
  closes the last two silent modes: dunning cron now SELF-HEALS non-durable
  `Final Invoice URL` rows (re-mint + stamp before send; helper
  `isDurableFinalInvoiceUrl`), and BOTH raw-URL fallback paths page Telegram
  the moment they fire. Sweep confirmed 0 other rows held raw URLs.
- Hard rule 3 now covers `/r/f/` too: **never email raw Stripe URLs — durable
  `/r/p/` (deposit) and `/r/f/` (final invoice) links only.**
- Split-pay wrinkle surfaced: two buyers splitting one share have ONE checkout
  for the full balance. Told them: one pays full, settle privately. If this
  recurs → build decision (two half-links).
- ManyChat: catchall "Instagram Default Reply" DELETED (was 0% CTR spam);
  HERD flow 3rd button swapped `👀 just lookin → /guide` ⇒ `🧢 rep the herd →
  buyhalfcow.com/merch` (IG caps DM cards at 3 buttons; follow-up-nudge slot
  requires an Opening-DM gate — rejected, would risk 47.5% CTR flow).
- Note: invoice emails' reply-to now uses `ref-<referralId>@` (my manual send
  did); platform's sendBuyerFinalInvoice still uses `inbox@` — minor
  attribution gap, not urgent.

## ⚡ ADDENDUM 2026-07-20 — read this first

### Shipped since the 07-19 sections below
- **[#410](https://github.com/benjibushes/bhc/pull/410)** `Deposit Invite Sent At` persists on send.
- **[#411](https://github.com/benjibushes/bhc/pull/411)** `Deposit Link Opened At` stamped on first `/r/p/` click (was a dead gauge — NEVER on every referral).
- **[#413](https://github.com/benjibushes/bhc/pull/413)** Phone REQUIRED on both rancher signup doors (`/apply` client+server, `self-submit` server, 10-digit min). Trigger: Vale Creek incident below.

### Corrections to 07-19 numbers (my audit script was buggy)
- Dead gauges: **63, not 111** (script missed `obj['Field'] = x` bracket-assign writes). Money-path: **3, not 9** — `Deposit Style` (12 reads), `Stripe Account Id` (5 reads — almost certainly should be `Stripe Connect Account Id`), `Amount` (2 reads — likely should be `Deposit Amount`). These 2 look like wrong-field-name bugs in money code → **fix next session** (read each call site, correct name).
- **RETRACTED: "circuit breaker cannot fire."** `Bounced`/`Complained` ARE written by `app/api/webhooks/resend/route.ts:95-96`. Code is fine. BUT—

### 🔴 The Resend webhook is NOT FIRING (proven live 07-20)
Nikki's setup email hard-bounced 17:34 (`552 mailbox not found`), and her
`Bounced` field is **still blank**. Event never reached the webhook.
→ Check Resend dashboard: endpoint `https://www.buyhalfcow.com/api/webhooks/resend`
registered? Events `email.bounced`/`email.complained` enabled? Signing secret
matches `RESEND_WEBHOOK_SECRET` in Vercel? (Route Telegrams on sig-fail — did Ben
get that alarm?) **Until fixed: suppression list never updates, dead addresses
get re-mailed, sender reputation erodes silently. Fix BEFORE grind-week volume.**

### 🔴 Final-invoice rail emails raw Stripe URLs (24h expiry) — Dana incident
`send-final-invoice/route.ts:292` writes `checkout.stripe.com/...` to
`Final Invoice URL` and emails it. Dana (Foodstead, **$2,249**, refId
`recOlGI4mMdSDsz9V`): link sent Jul 15, expired ~Jul 16, dunning cron then
reminded her Jul 19 **with the dead link**. She was active Jul 20 trying to pay.
- **Immediate (Ben/Beckie):** rancher-dashboard resend, or authenticated
  `POST /api/rancher/referrals/recOlGI4mMdSDsz9V/send-final-invoice?resend=true`
  (requireRancher — admin impersonate exists at `/api/admin/ranchers/[id]/impersonate`).
  New link ALSO dies in ~24h → resend while buyer is reachable.
- **Build:** port the deposit rail's durable `/r/p/` link pattern (#369) to
  send-final-invoice + make dunning reminders re-mint. Same landmine, second door.

### Vale Creek / Nikki — unreachable rancher
`reczvo7DeldetpLsR`, Plentywood MT, email `nikki_subley@yahoo.com` = dead
mailbox, **no phone on file**, Status Pending. Ben must find her another way
(Google/FB "Vale Creek Ranch Plentywood MT"), fix email, resend setup link.

### Money sitting RIGHT NOW (~$6,600)
- Dana **$2,249** (expired link — resend above).
- 6 buyers, **$4,350** total, LIVE valid links (tested Gail's: 302 → checkout):
  `buyhalfcow.com/checkout/<refId>/deposit` for recyQr4tkr0IrYmAz (Gail),
  recXfb3F256S5nVAt (Lori), recThxFWFgXwUo6EH (Meagan), recZybXaQco2rKNr8
  (Joann), recFrHUTbGA4G8P8P (Crystal — all Silverline, req Jul 2),
  recJ6BRQje2MHKR06 (Brent, Renick), recOlGI4mMdSDsz9V is Dana. Emails were
  DELIVERED (Resend-verified) — this is sales friction; text/call them.
  Note: Gail + Crystal's nudges were cap-suppressed (they heard less than the
  system thinks).
- **Dave PAID $650 live mid-session (Champion Valley)** — the deposit rail is
  proven end-to-end.

### Cap-exceeded suppressions (still open)
503 total (258 in July), **18 money emails** (`deposit_request_nudge_1/2`).
Templates ARE whitelisted (`lib/emailFrequencyGuard.ts:37-38,185`) — so either
suppressions predate that fix or the whitelist path is broken.
**First action next session: date the 18 via Airtable `createdTime`.**

### Grind week (Ben's KPI board, week of 07-20)
North star: **payable ranchers 6 → 16** (each ≈ +$162/mo; $10k/mo ≈ 60).
Daily: 40 dials / 10 convos / 2 made payable (legacy flip, NO Stripe SSN talk).
Call list: 39 warm ranchers (onboarded, not payable), demand-first:
**TX×7 (327 buyers waiting): Bear Musgrave, Rocking B, Crescent C, Wadsworth,
Cone, Matula, 4F · CA×3 (291): Anvil Bar, CG Cattle, JJ Branded · GA×5 (70) ·
CO×3 "Live" (108): High Lonesome, Next Horizon, All Natural.** Phones in
Ranchers table. Close line: "5 minutes, no SSN — paste the payment link you
already use, I flip you live today."
Before the 100k-follower partner posts: flip signup default tier_v2→legacy
(`app/api/apply/route.ts:234`, self-submit:255) + invert book-a-call gate
(`RancherSetupWizard.tsx:3450-3457`) — SSN wall killed 73% (11/15).

### ManyChat (done, live)
New single-CTA **HERD** flow LIVE (comment → 1 DM → 3 buttons → link), 33% CTR
vs old 14%. Old "IG Herd Funnel" fully STOPPED 07-20 (its DM trigger was
spamming every "hi/hey/?" DM with /links — that was the leak). Docs:
`docs/MANYCHAT-FLOWS.md` (rebuilt), `MANYCHAT_AI_CLOSER.md` (caged 2-turn AI).

### Next-session build order
1. Date the 18 money-email suppressions → fix whitelist path if recent.
2. Verify/fix the Resend webhook registration (bounce stamping).
3. Port durable links to final-invoice + dunning re-mint.
4. Fix `Stripe Account Id` / `Amount` wrong-field-name reads (3 money dead gauges).
5. Legacy-default + call-gate invert (pre-partner-post).
6. Watchdog cron + per-rancher go-live payment smoke test (spec below, Part 1c/1d — still valid).

---

---

## 1. VERIFIED TRUTHS (proven with live probes, not assumed)

**The deposit rail works end to end.** Dave (`recbnzdZB4MixIyh5`) paid $650 to
Champion Valley during this session. Confirmed via Resend: deposit-confirmed
email delivered to buyer, "💰 Dave just paid" delivered to rancher, rancher
accepted. No human intervention.

| Component | Status | How verified |
|---|---|---|
| Deposit API | ✅ works | `POST /api/checkout/deposit` reached `terms_required` — passed Connect, CSRF, buyer-ownership, already-paid, rancher/tier/bank/Connect-account, price floor |
| Deposit page | ✅ 200 | `GET /checkout/<refId>/deposit` |
| Pay links | ✅ valid 30d | Gail's 17-day-old `/r/p/<jwt>` → 302 to her checkout |
| `STRIPE_CONNECT_ENABLED` | ✅ true | endpoint returns 400 not 503 |
| Champion Valley Stripe | ✅ healthy | `charges_enabled`, `payouts_enabled`, requirements empty, bank attached, type=standard |
| Charge pattern | ✅ correct | direct charge + `application_fee_amount` (`lib/stripeConnect.ts:383,472`) — right for Standard accounts |
| Email delivery | ✅ delivered | Resend: last 20 sends ALL `delivered`. Domains `buyhalfcow.com`, `mail.`, `replies.` verified |

**Nothing on the money path is broken.** What was broken was the ability to SEE it.

---

## 2. SHIPPED THIS SESSION

| PR | What |
|---|---|
| [#403](https://github.com/benjibushes/bhc/pull/403) | `prp-` reply prefix — cold rancher replies route to the proven Resend Inbound webhook, forced rancher-side, excluded from buyer arm |
| [#404](https://github.com/benjibushes/bhc/pull/404) | prp replies stamp the prospect row (booking-brain bridge) |
| [#407](https://github.com/benjibushes/bhc/pull/407) | `BUYER_AUTORESPOND` defaults to `auto` |
| [#408](https://github.com/benjibushes/bhc/pull/408) | `/onboard` Telegram command — stuck ranchers as tap-to-Go-Live cards |
| [#410](https://github.com/benjibushes/bhc/pull/410) | **`Deposit Invite Sent At` now persists** |
| [#411](https://github.com/benjibushes/bhc/pull/411) | **`Deposit Link Opened At` now stamped on first click** (`/r/p/[token]`) |

Prospects dashboard (separate repo, deployed): IMAP triage retired → replies via
Resend Inbound `prp-<id>@replies.buyhalfcow.com`; send caps 10→25 / draft 12→28;
autopilot + autobook default `auto`; **money-domain failover REMOVED** (cold
sends HALT rather than ride `buyhalfcow.com`); inbox-rotation engine built
(`lib/inboxRotation.js`, 15 tests) but dormant until `OUTREACH_INBOXES` is set.

ManyChat: new single-CTA **HERD** flow LIVE (comment → one DM → 3 buttons →
right link), 33% CTR vs 14% on the old funnel. Old "IG Herd Funnel" **STOPPED**
(both triggers disabled — its DM trigger was firing on `hi/hey/?/want` and
spamming everyone who DM'd).

---

## 3. OPEN BUGS — ranked, all evidence-backed

### 3a. 🔴 The circuit breaker cannot fire
`Bounced` (read **25×**, written **0×**) and `Complained` (read **21×**, written
**0×**). The outreach circuit breaker gates on these. **It has never tripped
because it can't.** Live domain-reputation exposure while sending.

### 3b. 🔴 Money emails are being rate-capped
**503 `cap-exceeded` suppressions total, 258 in July.** 18 are money emails
(`deposit_request_nudge_1/2`) — including **Gail and Crystal**, two of the
stalled buyers. Their "please pay" email was silently killed.
Templates ARE whitelisted (`lib/emailFrequencyGuard.ts:37-38,185`) with a comment
saying this was already fixed once — so either the suppressions predate the fix
or **the whitelist isn't applied on that path.**
**FIRST ACTION: date those 18 suppressions via Airtable `createdTime`.** If any
are recent, money emails are still being capped.

### 3c. 🟡 111 dead gauges (fields read, never written)
9 on the money path: `Deposit Style`(12×), `Stripe Connect Connected At`(9×),
`Refunded Amount Cents`(5×), `Total Charged Cents`(5×), `Stripe Account Id`(5×),
`Amount`(2×), `Subscription Next Invoice At`(2×), `Stripe Subscription ID`(1×),
`Refund Reason`(1×).
Several are likely **duplicate-name bugs**: `Stripe Account Id` vs the real
`Stripe Connect Account Id`; `Amount` vs `Deposit Amount`. Code reads the wrong
name → `undefined` → silently treated as zero/false/never.
**Fix the class, not instances: wire the dead-gauge audit into CI** (script in
§6) so a money-path field that's read but never written fails the build.

### 3d. 🟡 Data integrity
- 11 referrals have `Commission Paid At` (a date) but `Commission Paid` = **$0**.
  **The books cannot say whether $2,920 of earned commission was collected.**
  Only Ben can answer this.
- **82** referrals stamped `2099-12` (sentinel garbage; breaks date sorting).
- 1,391 Closed Lost with **no Loss Reason** (auto-reaped by the chase-up cron).
- Same fact in two fields: `Cut Size` vs `Order Type`. Pick one, migrate, delete.

### 3e. 🟡 `ranch.buyhalfcow.com` = `status: failed` in Resend
Explains the Gmail/cold-send mess. Delete or re-verify. Not money-path.

---

## 4. BUSINESS STATE (real numbers)

- **$30,798** beef sold through BHC · **$2,920** commission earned · **18 closed deals** (since May)
- ~**$162** avg commission, ~**$1,711** avg sale → **~$1k/mo** currently
- **$10k/mo ≈ 62 deals/mo ≈ ~60 payable ranchers.** Currently **6**.
  → **North-star metric: payable ranchers, 6 → 60.** Every one ≈ +$162/mo.
- 2,629 buyers, 1,953 waiting. **Demand is not the constraint.**
- 177 live referrals (171 touched this month), **122 on ranchers who can take money**
- 6 still Awaiting Payment (~$4,350) — emails **delivered**, unpaid = sales friction, needs a text/call
- 80 ranchers: 6 payable, 49 onboarded-but-not-payable, **32 of those in demand states**

**Fastest supply path:** partner with ~100k beef-industry followers. Before he
posts, flip signup default from `tier_v2` to **legacy** — tier_v2 forces Stripe
SSN+bank before go-live and **73% of ranchers died there** (11 of 15). Legacy
goes live on e-sign with their own payment link; Connect becomes a later upgrade.
Also invert the book-a-call gate (`RancherSetupWizard.tsx:3450-3457`).
Est. impact: ~15% → ~40% of a cold flood reaching live.

---

## 5. CORRECTIONS (things I got wrong — do not repeat)

I was wrong **five times**, always the same way: **guessing an Airtable field
name instead of reading the schema.**

1. "Close is broken" — queried `Deposit Paid At` (new-model field) against
   legacy invoice deals. **Wrong.** Real fields: `Sale Amount`, `Commission Due`,
   `Commission Paid`.
2. "Cross-state blocked the deposit" — Champion Valley serves NE/**CO**/KS with
   `Admin Approved Multi-State`. **Wrong.**
3. "No deposit links exist" — queried `Deposit Link`; the real field is
   **`Deposit Checkout URL`**, and 6 of 7 have valid ones. **Wrong.**
4. "Invites never sent" — `Deposit Invite Sent At` was simply never *written*.
   Email Sends log shows `buyer_deposit_invoice` **sent**. **Wrong.**
5. "Buyers aren't receiving emails" — Resend says **delivered**. The open field
   was a dead gauge. **Wrong.**

**Rule for any future session: read the table schema before asserting anything
about a field. A blank field means "nothing writes here" at least as often as
it means "this didn't happen."**

`docs/MONEY-TRUTH-INVARIANTS.md` Part 1 is built on #3/#4 and is **wrong** —
links ARE minted and ARE sent. Its invariant idea is still good; rewrite it
around dead gauges + money-email capping.

---

## 6. NEXT ACTIONS, in order

1. **Date the 18 money-email suppressions** (§3b). Decides if deposits are still being capped.
2. **Fix `Bounced`/`Complained`** (§3a) so the circuit breaker can actually fire.
3. **Wire the dead-gauge audit into CI** (§3c) — kills the whole class:
   ```
   reads:  /\[\s*'([A-Z][A-Za-z0-9 \/&\-]{3,40})'\s*\]/     (something['Field'])
   writes: /'([A-Z][A-Za-z0-9 \/&\-]{3,40})'\s*:/           (object-literal key)
   fail build if a money-path field appears in reads and never in writes
   ```
4. **Resolve the $2,920** (§3d) — was it collected? Ben-only.
5. **Legacy-default + invert call gate** (§4) before the partner posts.
6. Watchdog cron (Awaiting Payment >2h without a sent invite → Telegram) +
   go-live payment smoke test per rancher.

### Ben-side, not code
- Text the 6 unpaid buyers (~$4,350). Two had their nudge suppressed.
- Decide Connect-vs-legacy per rancher for the 49 (his relationships — never batch-flip).
- `OUTREACH_INBOXES` roster + cold domains if the outreach machine scales.

> 07-22c: deploy-drift alarm was prod(#446)=12c9452 vs main(#447 docs-only)=f732006 — Vercel skipped the docs-only production build. This commit re-syncs prod SHA to HEAD. Runtime delta was zero.
